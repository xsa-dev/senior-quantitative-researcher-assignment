.venv/
.pytest_cache/
__pycache__/
*.pyc
*.egg-info/
.DS_Store
.cmux/

# Locally generated Google Doc upload source; the committed source is
# docs/submission_ru.md plus scripts/create_submission_doc_ru.py.
docs/submission_ru.html

# Raw assignment files stay local / external storage only.
documents/

# Generated result artifacts are delivered via Google Drive, not GitHub.
# Keep only the directory skeleton with .gitkeep files so README/docs can point
# to stable local target paths.
outputs/**
!outputs/
!outputs/**/
!outputs/**/.gitkeep
